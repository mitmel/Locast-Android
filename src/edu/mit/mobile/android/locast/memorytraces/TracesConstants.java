package edu.mit.mobile.android.locast.memorytraces;

public class TracesConstants {

	/**
	 * Unfortunately, the Android style system doesn't support typefaces, so this is needed to use a
	 * custom typeface.
	 */
	public static final String TYPEFACE_TITLE = "oswald.ttf";
}
